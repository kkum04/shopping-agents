# mission Review Checklist: delivered 로그인 연동 — 세션에 토큰 보관, 고객 API Bearer 호출

**Purpose**: mission 문서 품질 검증
**Created**: 2026-09-11
**Feature**: ./mission.md

## 완전성 (Completeness)

- [x] CHK001 로그인 성공·실패(잘못된 자격증명) 두 경로가 모두 인수 조건으로 정의되어 있는가? — ✅ AC1 성공·실패(401/404) 경로 정의
- [x] CHK002 토큰이 나타나면 안 되는 위치(프롬프트·툴 인자·툴 결과·로그·응답 본문)가 빠짐없이 열거되어 있는가? — ✅ AC3 5곳 열거 + NFR 보안(state_document·트랜스크립트)
- [x] CHK003 게스트 세션에서 계속 허용되는 기능(검색·상세)과 막히는 기능(계정 필요)이 구분되어 있는가? — ✅ AC4 계정 필요 거절 / 검색·상세 허용 구분
- [x] CHK004 세션 시작·로그인·로그아웃 세 경로의 요청/응답 계약(필드)이 FE 티켓이 구현할 수 있을 만큼 적혀 있는가? — ⚠️ 경로 URL·실패 응답 형식·로그아웃 요청 없음 → ✅ 해소: 데이터 요구사항에 /api/session/login·/logout 경로, 401/502 응답 형식, 로그아웃 요청 추가 (2026-09-11)

## 명확성 (Clarity)

- [x] CHK005 "게스트로 되돌린다"가 같은 세션 유지인지 새 세션인지 명시되어 있는가? — ✅ 만료=같은 세션 유지, 로그아웃=토큰만 삭제(보강 후 같은 세션)
- [x] CHK006 리프레시 토큰의 처리(보관만 / 사용 안 함)가 명시되어 있는가? — ✅ Background·AC1·Out of Scope에 보관만/미사용 명시
- [x] CHK007 인증 게이트웨이와 고객 API의 기본 환경과 전환 방법이 구체적 값(URL·환경변수)으로 적혀 있는가? — ✅ NFR 호환성 + Q1 확정 (스테이징 기본, DELIVERED_AUTH_URL·DELIVERED_CUSTOMER_API_URL)

## 일관성 (Consistency)

- [x] CHK008 frontmatter `acceptance`의 EARS 항목이 본문 AC와 1:1로 대응하고 번호가 어긋나지 않는가? — ⚠️ frontmatter AC1·AC3이 본문보다 좁음 → ✅ 해소: EARS AC1(401/502)·AC3(state_document·트랜스크립트·응답 본문)·AC5(툴 결과 문자열) 갱신 (2026-09-11)
- [x] CHK009 Out of Scope 항목이 User Stories/AC 어디와도 충돌하지 않는가 (예: 리프레시 흐름, 소셜 로그인)? — ✅ Out of Scope와 스토리/AC 충돌 없음
- [x] CHK010 Background의 `apiKey` 가정이 AC나 NFR과 모순되지 않는가? — ✅ apiKey 가정은 AC2·NFR과 정합

## 커버리지 (Coverage)

- [x] CHK011 티켓 작업 범위 6개 항목(토큰 보관·로그인 경로·Bearer 공통 경로·프로필 컨텍스트·게스트 안내·만료 처리)이 각각 최소 하나의 AC로 덮이는가? — ✅ 작업 범위 6항목 → AC1·AC2·AC6·AC4·AC5 매핑
- [x] CHK012 후속 티켓(RBD-8277 장바구니)이 의존하는 계약(Bearer 공통 경로, 게스트 예외)이 이 mission에 정의되어 있는가? — ✅ Bearer 공통 경로(AC2)·게스트 예외(AC4) 정의

## 엣지 케이스 (Edge Cases)

- [x] CHK013 만료 토큰 감지 조건(응답 메시지 문자열)과 감지 시 동작이 정의되어 있는가? — ✅ AC5 감지 조건 `Expired Token` + 동작
- [x] CHK014 인증 게이트웨이가 다운되거나 5xx를 돌려줄 때의 동작(로그인 실패 처리)이 정의되어 있는가? — ❌ 게이트웨이 5xx/타임아웃 동작 없음 → ✅ 해소: AC1에 502 처리 추가, 계약에 `auth_unavailable` (2026-09-11)
- [x] CHK015 로그인한 세션이 만료된 뒤 다시 로그인하면 기존 세션과 어떻게 연결되는지(같은 세션 재사용 여부)가 정의되어 있는가? — ⚠️ 재로그인 시 세션 연결 미정의 → ✅ 해소: /login이 X-Session-Id로 같은 세션에 토큰을 붙여 대화 승계 (2026-09-11)

## 측정 가능성 (Measurability)

- [x] CHK016 모든 AC가 관찰 가능한 동작·상태(응답 코드, 문자열 존재 여부, 테스트 통과)로 쓰여 있고 "적절히", "충분히" 같은 모호 표현이 없는가? — ⚠️ 실패 응답 코드·재로그인 안내 관찰 기준 미지정 → ✅ 해소: 401/502 코드, 툴 결과 `다시 로그인` 문자열 (2026-09-11)
- [x] CHK017 테스트 AC(AC7)가 어떤 시나리오를 어떤 도구로 검증하는지 명시되어 있는가? — ✅ AC7 도구·시나리오 명시

## Notes

- 2026-09-11 격리 검증(subagent): ✅12 · ⚠️4 · ❌1(엣지 케이스, non-blocking) → 판정 passed. ⚠️/❌ 5건은 mission.md 보강으로 같은 날 해소.

- 완료 시 `[x]` 표시
- 발견 사항은 인라인 코멘트로 추가
- 항목은 절대 삭제/재번호 금지 — append only
